[project]
name = "cursor-x-amass"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Martin Skrolsvik", email = "maskr1070@uib.no" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
cursor-x-amass = "cursor_x_amass:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
